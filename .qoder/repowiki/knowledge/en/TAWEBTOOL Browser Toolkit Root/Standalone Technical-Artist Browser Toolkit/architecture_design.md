Two complementary delivery patterns coexist under `tools_html/`, each page being a self-contained HTML file:
- **Static single-page tools** (e.g. `3d_city.html`, `TA_wiki.html`): the HTML owns its DOM tree directly and loads shared `menu.js` + `waifu.js` plus its own CSS/JS in `<head>` / `<body>`.
- **Workbench-injected tools** (e.g. `chatgpt.html`, `video_cut.html`, `ai_draw.html`): the HTML contains only an empty `<div id="panel" data-local-tool="...">`; `js/local_workbench.js` reads that attribute at `DOMContentLoaded`, builds a `.tool-shell` host, then dispatches to per-tool init functions (`initChatTool`, `initVideoCut`, `initAiDrawTool`) which call back into the tool module's exported `window.initXxxTool` if available, otherwise fall back to inline stubs or redirect to `designtool.site` via `designToolLinks`.
- Shared shell lives outside the scope: `js/menu.js` injects the left navigation from a single `MENU_DATA` source of truth, `css/common.css` defines the sidebar/top-search/main-panel layout, and `css/local_workbench.css` styles the injected workbench shell.
- Tool modules are independent JS files under `js/` with no cross-imports; they communicate with the host only through `window.initXxxTool` callbacks or by reading/writing elements inside their assigned `#panel` subtree.
- External libraries are loaded from CDN per-tool (Cesium, Leaflet, model-viewer, marked, DOMPurify, Fuse.js) rather than centralized, keeping each HTML entry point fully portable.